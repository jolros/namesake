<div class="posts">
	<% @posts.each do |post| %>
	<div class="page-header">
	  <h1><%= post.title %> <small><%= post.created_at %></small></h1>
	</div>
		<div class="post">
			<%# post.rendered_content.html_safe %>
			<%= markdown(post.content) %>
		</div>
	<% end %>
</div>